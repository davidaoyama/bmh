# BMH Portfolio — Implementation Plan

## Checklist

- [ ] 1. Install dependencies (`framer-motion`, `lucide-react`)
- [ ] 2. Update `app/layout.tsx` — swap Geist for Inter font, update metadata
- [ ] 3. Update `app/globals.css` — simplify for light-only minimalist theme
- [ ] 4. Create `components/Navbar.tsx` — fixed top bar with BMH logo + visible links
- [ ] 5. Build `app/page.tsx` — Hero section, project data, project grid with Framer Motion animations
